package com.company.rootFolder.model;

//TODO : WIP
public class StatMove extends Move{

    public StatMove(String name, ElementType elementType, int accuracy, int priority, int ammunition, MoveType moveType) {
        super(name, elementType, accuracy, priority, ammunition, moveType);
    }
}
